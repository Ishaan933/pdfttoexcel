services:
  - type: web
    name: pdf-to-excel-service
    runtime: python
    buildCommand: |
      apt-get update && apt-get install -y openjdk-11-jdk
      pip install -r requirements.txt
    startCommand: python app.py
